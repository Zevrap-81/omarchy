(defcfg  
  process-unmapped-keys yes
)

(defsrc
  caps s d f j k l ; 
)

(defvar
  tap-time 175
  hold-time 250
)

(defalias
  escctrl (tap-hold 100 100 esc lctl)
  s (multi  (tap-hold $tap-time $hold-time s lmet))
  d (multi  (tap-hold $tap-time $hold-time d lsft))
  f (multi  (tap-hold $tap-time $hold-time f lctl))
  j (multi  (tap-hold $tap-time $hold-time j rctl))
  k (multi  (tap-hold $tap-time $hold-time k rsft))
  l (multi  (tap-hold $tap-time $hold-time l rmet))
  ; (multi  (tap-hold $tap-time $hold-time ; ralt))
)

(deflayer base
  @escctrl @s @d @f @j @k @l @; 
)
